import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    custom: {
      50: "#FDFFFF",
      100: "#DE7C5A",
      200: "#B10F2E",
      300: "#570000",
      400: "#280000",
    },
  },
});

export default theme;
